{
  "routes": [
    {
      "method": "GET",
      "path": "/product",
      "handler": "product.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/product",
      "handler": "product.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/product",
      "handler": "product.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
